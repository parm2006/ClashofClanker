[project]
name = "clash-of-clans-bot-vision"
version = "0.1.0"
description = "Managed runtime for the bot's OpenCV vision hook."
requires-python = ">=3.13,<3.14"
dependencies = [
    "opencv-python>=4.12,<5",
]
